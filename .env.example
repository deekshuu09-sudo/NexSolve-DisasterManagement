# NexSolve Disaster Management System — Environment Configuration Template
# Copy to .env for runtime environment overrides

# BACKEND CONFIGURATION
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
FRONTEND_URL=https://your-nexsolve-frontend.onrender.com

# FRONTEND CONFIGURATION (Set in frontend/.env.local or build environment)
# Leave empty for relative /api routing under same-domain deployment / proxy
VITE_API_URL=https://your-nexsolve-backend.onrender.com
